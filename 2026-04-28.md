# 2026-04-28 2팀 The Truth Filtering Engine

작성자 : 팀장 최현석
작성 시간 : 2026-04-28 17시 ~ 18시

1. 진행한 내용 :
   - BERT/Electra 모델 설정 파일과 OpenVINO 모델 파일을 추가하고, 분석 모드 provider를 구현했다.
   - 리뷰 상세 화면에 AI 분석 카드, 워드 클라우드, 리뷰 리스트, 데이터 없음 상태 UI를 추가했다.
   - 모델/LLM 기반 리뷰 상세 페이지를 상/중/하 구조로 구현하고 Supabase 연동을 보강했다.
   - 분석 모델의 binary 결과를 사용자 점수(score)로 변환하는 로직을 수정했다.
   - 카카오 지도 웹 뷰, map point 모델, 지도 설정 및 의존성을 업데이트했다.
   - 전체 UI를 수정하고 로컬 북마크 저장 기능을 추가했다.
   - 카카오 API 음식점 분류에 따른 헤더 이미지와 썸네일 이미지를 추가했다.
   - AI 추천 기능을 위한 백엔드 라우터, 모델, provider, 화면, 추천 카드 위젯을 추가했다.
   - 서버 IP를 localhost로 정리하고, merge 충돌 및 ignore 대상 추적 파일을 정리했다.

2. 발생한 이슈 :
   - 분석 모델의 원본 출력이 binary 형태라 사용자 화면에 바로 노출하기 어려웠다.
   - 지도 관련 의존성, 카카오 지도 설정, 상세 화면 UI 변경이 동시에 진행되면서 merge 충돌이 반복됐다.
   - `.env`, `__pycache__` 등 추적하면 안 되는 파일이 Git 이력에 포함되어 있었다.
   - 로컬 서버 주소와 앱 API 호출 주소가 맞지 않아 통합 테스트가 불안정했다.

3. 이슈 해결 방법 :
   - binary 출력값을 점수로 변환하는 서비스 로직을 추가해 화면 표시 기준을 정리했다.
   - 지도 관련 파일과 pubspec 의존성을 함께 정리하고, 충돌 파일은 기능 단위로 분리해 재통합했다.
   - ignore 대상 파일을 Git 추적에서 제거하고 `.gitignore` 기준을 보강했다.
   - API base URL을 localhost 기준으로 맞춰 프론트와 백엔드 연동을 단순화했다.

4. 내일의 계획 :
   - 지도 컨트롤 버튼의 포인터 인터랙션 문제를 점검한다.
   - 북마크/검색 결과에서 음식점을 클릭했을 때 지도 위치로 바로 이동하도록 UX를 개선한다.
   - 카카오맵 로딩 순서를 안정화한다.
   - 상세보기 클릭 시 자동 분석이 실행되도록 하고 LLM 판별 기준을 정교화한다.
   - Colab 학습 코드와 프로젝트 문서를 정리한다.
