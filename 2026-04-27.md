# 2026-04-27 2팀 The Truth Filtering Engine

작성자 : 팀장 최현석
작성 시간 : 2026-04-27 17시 ~ 18시

1. 진행한 내용 :
   - 카카오 지도 API 오류를 수정하고 지도 화면, 지도 provider, 하단 시트, 진실 점수 마커 표시를 개선했다.
   - 앱 헤더 UI를 수정하고 로고 및 공통 앱바 구조를 정리했다.
   - 네이버 장소 API 연동을 위한 `places` 라우터를 추가하고 지도 화면에서 장소 데이터를 사용할 수 있도록 연결했다.
   - 상세 화면의 블로그 리뷰 원문 처리 방식을 수정했다.
   - 상세 버튼과 분석하기 버튼을 LLM, Electra, Supabase와 연동하여 리뷰 분석 흐름을 구현했다.
   - 음식점 표시 로직을 수정해 지도와 상세 화면의 데이터 표현을 안정화했다.

2. 발생한 이슈 :
   - 카카오 지도 API 설정과 앱 내부 지도 상태 관리가 맞지 않아 지도 로딩 및 음식점 표시가 불안정했다.
   - LLM, Electra, Supabase, 네이버 API를 함께 연결하면서 백엔드 라우터와 프론트 provider 간 데이터 흐름이 복잡해졌다.
   - 원본 블로그 글 링크 처리와 상세 화면 전환 방식에 오류가 있었다.

3. 이슈 해결 방법 :
   - 지도 provider와 화면 상태를 정리하고 카카오 지도 관련 설정 파일을 수정했다.
   - `backend/routers/search.py`, `backend/routers/places.py`, `backend/services/naver_service.py`, `backend/services/electra_service.py`, `backend/services/supabase_service.py`를 중심으로 API 책임을 분리했다.
   - 상세 화면 provider와 블로그 리스트 화면을 수정해 분석 결과 및 원본 리뷰 접근 흐름을 맞췄다.

4. 내일의 계획 :
   - 모델/LLM 설정을 고도화하고 분석 모드를 앱에서 선택할 수 있도록 정리한다.
   - 리뷰 상세 화면에 워드 클라우드와 분석 카드 UI를 통합한다.
   - 분석 결과를 사용자에게 이해 가능한 점수 형태로 변환한다.
   - 지도 기능, 북마크, AI 추천 기능을 병행 구현한다.
